# T056 Recovery packages

Local verification covers bounded ZIP inspection, canonical path rejection, invalid document rejection, owner isolation, inspection-hash comparison, and confirmation-only project creation. The migration is intentionally not deployed by this worktree. PDF attachment extraction requires the integration worker's qpdf adapter; no ordinary PDF is treated as a recovery package.

# OS and tooling
.DS_Store
node_modules/
next-env.d.ts
.next/
# NEXT_DIST_DIR 开出来的验证性构建产物（.next-verify / .next-deploycheck 等），
# 它们和 .next 一样是产物，不该进仓库。
.next-*/
.vercel/
.trigger/
dist/
build/
coverage/
playwright-report/
test-results/
.cache/
.tmp/
tmp/
*.tsbuildinfo
*.log
*.swp

# Secrets (examples are deliberately blank)
.env
.env.*
!.env.example
*.pem
*.key

# User data and local operational artifacts
/uploads/
/exports/
/backups/
supabase/.temp/
supabase/.branches/
*.dump
*.sql.gz
.vercel
